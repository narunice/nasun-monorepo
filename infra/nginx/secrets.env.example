# Template for infra/nginx/secrets.env.
# Real values are NOT committed (.env.* gitignored).
# Get the actual values from prod EC2 /etc/nginx/conf.d/{nasun,pado.finance}.conf
# or from the secure handoff doc.

CF_ORIGIN_SECRET_NASUN=__SET_ME__
CF_ORIGIN_SECRET_PADO=__SET_ME__
