[package]
name = "nasun_tier"
edition = "2024"
# published-at = "0x..."  # Phase 4 publish 후 추가

[dependencies]
# Sui and std are implicit system dependencies in the new package system

[addresses]
nasun_tier = "0x0"

[environments]
devnet = "272218f1"
nasun-devnet = "272218f1"
