{
  "name": "get-supply-count-lambda",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node18 --outfile=dist/index.js --external:aws-sdk",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "aws-sdk": "^2.1358.0"
  },
  "devDependencies": {
    "esbuild": "^0.17.0",
    "typescript": "^5.1.0",
    "@types/aws-lambda": "^8.10.119",
    "@types/node": "^20.3.1"
  }
}
