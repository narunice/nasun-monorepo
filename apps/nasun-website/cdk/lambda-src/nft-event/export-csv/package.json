{
  "name": "export-csv",
  "version": "1.0.0",
  "description": "OpenSea CSV Export Lambda - 화이트리스트를 OpenSea 형식 CSV로 변환 및 S3 업로드",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "rebuild": "npm run clean && npm run build"
  },
  "keywords": ["nft", "opensea", "csv", "export"],
  "author": "NASUN",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.670.0",
    "@aws-sdk/client-s3": "^3.670.0",
    "@aws-sdk/lib-dynamodb": "^3.670.0",
    "@aws-sdk/util-dynamodb": "^3.670.0",
    "@aws-sdk/s3-request-presigner": "^3.670.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.0.0",
    "typescript": "^5.6.3"
  }
}
