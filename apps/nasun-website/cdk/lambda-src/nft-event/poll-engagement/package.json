{
  "name": "poll-engagement",
  "version": "1.0.0",
  "description": "Background engagement poller — caches liking_users and retweeted_by into DynamoDB",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "rebuild": "npm run clean && npm run build"
  },
  "keywords": ["nft", "event", "engagement", "polling"],
  "author": "NASUN",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.670.0",
    "@aws-sdk/lib-dynamodb": "^3.670.0",
    "twitter-api-v2": "^1.17.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.0.0",
    "typescript": "^5.6.3"
  }
}
