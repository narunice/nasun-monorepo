{
  "name": "auth-sui-additional",
  "version": "1.0.0",
  "description": "JWT-authed Lambda for verifying additional Sui addresses via personal-message signature + per-app binding (Nasun website)",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "jest"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.x",
    "@aws-sdk/lib-dynamodb": "^3.x",
    "@mysten/sui": "^1.45.2",
    "jose": "^5.x"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.x",
    "@types/node": "^20.x",
    "typescript": "^5.x"
  }
}
