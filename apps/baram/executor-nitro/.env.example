# Baram TEE Executor Environment Variables

# Required: OpenAI API key for AI inference
OPENAI_API_KEY=sk-...

# Optional: Enclave address (for Host)
ENCLAVE_HOST=localhost
ENCLAVE_PORT=5050

# Optional: Host HTTP server port
HOST_PORT=3000
