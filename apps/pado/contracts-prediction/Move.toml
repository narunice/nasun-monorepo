[package]
name = "pado_prediction"
edition = "2024"
version = "0.0.1"
# published-at removed for v5 fresh publish (2026-05-20)

[dependencies]
devnet_tokens = { local = "../../../packages/devnet-tokens" }

[addresses]
prediction = "0x0"

[environments]
devnet = "272218f1"
nasun-devnet = "272218f1"
