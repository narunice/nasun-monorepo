// Browser stub for Node.js https module
// Workaround: @ledgerhq/live-network uses require("https") in ESM build
export class Agent {
  constructor(_opts?: Record<string, unknown>) {}
}
export default { Agent };
