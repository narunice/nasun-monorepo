import ErrorBoundary from "../components/layout/ErrorBoundary";

export default function IPsPage() {
  return (
    <ErrorBoundary>
      <main className="min-h-screen bg-ivory text-black flex items-center justify-center">
        <h1 className="text-2xl">IPs Page (Coming Soon)</h1>
      </main>
    </ErrorBoundary>
  );
}
